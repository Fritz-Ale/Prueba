(()=>{'use strict';const $=q=>document.querySelector(q);const db={get(k,d){try{return JSON.parse(localStorage.getItem(k))??d}catch{return d}},set(k,v){localStorage.setItem(k,JSON.stringify(v))}};const safe=s=>String(s??'').replace(/[<>&"']/g,m=>({'<':'&lt;','>':'&gt;','&':'&amp;','"':'&quot;',"'":'&#039;'}[m]));const session=db.get('bhs_session',null);if(!session||session.role!=='admin'){location.href='../login.html';return}function products(){const list=$('#adminProducts');if(!list)return;const ps=db.get('bhs_products',[]);list.innerHTML=ps.map(p=>`<div class="card"><b>${safe(p.name)}</b><p>${safe(p.cat)} · $${Number(p.price||0).toLocaleString('es-CL')}</p><button class="btn secondary del" data-id="${p.id}">Eliminar</button></div>`).join('');document.querySelectorAll('.del').forEach(b=>b.onclick=()=>{db.set('bhs_products',ps.filter(p=>p.id!==b.dataset.id));products();});}function addProduct(){const f=$('#productForm');if(!f)return;f.addEventListener('submit',e=>{e.preventDefault();const ps=db.get('bhs_products',[]);ps.unshift({id:'p'+Date.now(),name:safe(f.name.value),cat:safe(f.cat.value),price:Number(f.price.value),emoji:safe(f.emoji.value||'🛍️'),tag:safe(f.tag.value||'Nuevo')});db.set('bhs_products',ps);f.reset();products();});}function orders(){const f=$('#orderForm'),list=$('#adminOrders');if(!f)return;const render=()=>{const os=db.get('bhs_orders',[]);list.innerHTML=os.map(o=>`<div class="card"><b>${safe(o.code)}</b><p>${safe(o.email)}</p><p>${safe(o.status)} · ${safe(o.carrier)} · ${safe(o.tracking)}</p></div>`).join('')};f.addEventListener('submit',e=>{e.preventDefault();const os=db.get('bhs_orders',[]);os.unshift({code:safe(f.code.value),email:safe(f.email.value.toLowerCase()),status:safe(f.status.value),carrier:safe(f.carrier.value),tracking:safe(f.tracking.value),steps:['Compra confirmada',safe(f.status.value)]});db.set('bhs_orders',os);f.reset();render();});render();}$('#logout')?.addEventListener('click',()=>{localStorage.removeItem('bhs_session');location.href='../index.html'});products();addProduct();orders();})();
